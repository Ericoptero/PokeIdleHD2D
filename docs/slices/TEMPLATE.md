# NNN — <one-line title>

Status: proposed | in-progress | review | done          Branch / commit: …

## Why
One paragraph. Cite the `docs/STATUS.json` `open` entry id or the DECISIONS # it answers, if any.

## Inspected before writing this slice
The files read, and the fact in each that matters, as `path:line — fact`. A slice written
without this section was written from memory, and memory is what drifts.

## Files / modules affected

## Expected behaviour
Observable: an event on the bus, a return value, a save field, a pixel metric, a gate stage.

## Acceptance criteria
Each one checkable by a command or a test name. "Works" is not a criterion.

## Tests required
Unit `*.test.js` / selftest check / flow `*.spec.js` / regress row — named, not described.

## Verification in the real application
The URL, the `__HOOKS__` steps, or `npm run shot -- …` and what to look at in the frame.

## Docs to touch
ARCHITECTURE section, DECISIONS entry (only if a choice constrains future code), STATUS `open`.

## Out of scope

## Result
Filled in when done: the gate summary (stage / status / seconds), what moved, what was left out.
